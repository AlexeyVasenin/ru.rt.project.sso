# datasource, postgres
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
# ddl, hibernate
# spring.jpa.generate-ddl=true
# spring.jpa.hibernate.ddl-auto=none
spring.liquibase.enabled=false
# liquibase
spring.liquibase.change-log=classpath:/db/changelog/db.changelog-master.xml


# keycloak config
keycloak.auth-server-url=https://keycloak:8180/auth
keycloak.realm=sso_realm
keycloak.resource=cinema-app
keycloak.credentials.secret=192f9bc9-7617-4b97-a549-92dc40d091be
keycloak.principal-attribute=preferred_username
keycloak.use-resource-role-mappings=true

# keycloak constraints (no spring security yet)
keycloak.security-constraints[0].authRoles[0]=welcome_access
keycloak.security-constraints[0].securityCollections[0].patterns[0]=/users/*